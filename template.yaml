AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Kitties got claws...beware

Globals:
  Function:
    Timeout: 30
    Runtime: nodejs24.x

Resources:
  KittenClawsFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: dist/lambda.handler
      CodeUri: .
      Description: Kitties got claws...beware
      Architectures:
        - x86_64
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME_KITTIES: !Ref KittiesTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref KittiesTable
      Events:
        Health:
          Type: Api
          Properties:
            Path: /health
            Method: get
        ListKittenClaws:
          Type: Api
          Properties:
            Path: /kitties
            Method: get
        GetKittenClaws:
          Type: Api
          Properties:
            Path: /kitties/{id}
            Method: get
        CreateKittenClaws:
          Type: Api
          Properties:
            Path: /kitties
            Method: post
        UpdateKittenClaws:
          Type: Api
          Properties:
            Path: /kitties/{id}
            Method: patch
        DeleteKittenClaws:
          Type: Api
          Properties:
            Path: /kitties/{id}
            Method: delete

  KittiesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: kitties
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST

Outputs:
  KittenClawsApi:
    Description: API Gateway endpoint base URL
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/"
  KittenClawsFunction:
    Description: Lambda Function ARN
    Value: !GetAtt KittenClawsFunction.Arn
