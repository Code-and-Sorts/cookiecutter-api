namespace {{project_class_name}}.Api.Interfaces;

using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;

public interface IFirestoreContext<T> where T : class
{
    Task<T?> GetAsync(string id, CancellationToken ct = default);

    Task<IReadOnlyList<T>> GetListAsync(string field, object value, CancellationToken ct = default);

    Task SetAsync(string id, T item, CancellationToken ct = default);
}
