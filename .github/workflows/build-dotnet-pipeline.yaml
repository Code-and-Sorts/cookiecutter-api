name: Dotnet Copier API Build

on:
  push:
    paths:
      - dotnet/**
      - .github/workflows/build-dotnet-pipeline.yaml
      - .github/actions/setup-copier-template/**
    branches:
      - main
  pull_request:
    paths:
      - dotnet/**
      - .github/workflows/build-dotnet-pipeline.yaml
      - .github/actions/setup-copier-template/**
    branches:
      - main

jobs:
  build-dotnet:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
        template-type:
          - dotnet
        cloud-service:
          - "Azure Function App"
          - "GCP Cloud Function"
          - "AWS Lambda"
        dotnet-version:
          - "10.x"
        fixture:
          - single
        include:
          - os: ubuntu-latest
            template-type: dotnet
            cloud-service: "Azure Function App"
            dotnet-version: "10.x"
            fixture: multi
          - os: ubuntu-latest
            template-type: dotnet
            cloud-service: "GCP Cloud Function"
            dotnet-version: "10.x"
            fixture: multi
          - os: ubuntu-latest
            template-type: dotnet
            cloud-service: "AWS Lambda"
            dotnet-version: "10.x"
            fixture: multi
          - os: ubuntu-latest
            template-type: dotnet
            cloud-service: "Azure Function App"
            dotnet-version: "10.x"
            fixture: edge
          - os: ubuntu-latest
            template-type: dotnet
            cloud-service: "GCP Cloud Function"
            dotnet-version: "10.x"
            fixture: edge
          - os: ubuntu-latest
            template-type: dotnet
            cloud-service: "AWS Lambda"
            dotnet-version: "10.x"
            fixture: edge
      fail-fast: false
    steps:
      - name: Check out
        uses: actions/checkout@v7

      - name: Setup Copier Template
        uses: ./.github/actions/setup-copier-template
        with:
          template-language: ${{ matrix.template-type }}
          template-cloud-service: ${{ matrix.cloud-service }}
          resources-fixture: ${{ matrix.fixture }}

      - name: Setup dotnet ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: ${{ matrix.dotnet-version }}

      - name: Install dependencies
        run: make install
        working-directory: KittenClaws

      - name: Build
        run: make build
        working-directory: KittenClaws

      - name: Run lint
        run: make lint
        working-directory: KittenClaws

      - name: Test with the dotnet CLI
        run: make test-unit
        working-directory: KittenClaws

      - name: Run dependency vulnerability scan
        continue-on-error: true
        run: |
            set +e
            make audit 2>&1 | tee audit.log
            status=$?
            set -e
            {
              echo "### .NET dependency vulnerability scan (${{ matrix.cloud-service }}, ${{ matrix.fixture }} resources)"
              echo ""
              echo '```'
              cat audit.log
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
            exit $status
        working-directory: KittenClaws
        shell: bash
