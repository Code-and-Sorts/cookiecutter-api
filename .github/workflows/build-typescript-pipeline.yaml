name: Typescript Copier API Build

on:
  push:
    paths:
      - typescript/**
      - .github/workflows/build-typescript-pipeline.yaml
      - .github/actions/setup-copier-template/**
    branches:
      - main
  pull_request:
    paths:
      - typescript/**
      - .github/workflows/build-typescript-pipeline.yaml
      - .github/actions/setup-copier-template/**
    branches:
      - main

jobs:
  build-typescript:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
        template-type:
          - typescript
        cloud-service:
          - "Azure Function App"
          - "GCP Cloud Function"
          - "AWS Lambda"
        node-version:
          - "24.x"
        fixture:
          - single
        include:
          - os: ubuntu-latest
            template-type: typescript
            cloud-service: "Azure Function App"
            node-version: "24.x"
            fixture: multi
          - os: ubuntu-latest
            template-type: typescript
            cloud-service: "GCP Cloud Function"
            node-version: "24.x"
            fixture: multi
          - os: ubuntu-latest
            template-type: typescript
            cloud-service: "AWS Lambda"
            node-version: "24.x"
            fixture: multi
          - os: ubuntu-latest
            template-type: typescript
            cloud-service: "Azure Function App"
            node-version: "24.x"
            fixture: edge
          - os: ubuntu-latest
            template-type: typescript
            cloud-service: "GCP Cloud Function"
            node-version: "24.x"
            fixture: edge
          - os: ubuntu-latest
            template-type: typescript
            cloud-service: "AWS Lambda"
            node-version: "24.x"
            fixture: edge
      fail-fast: false
    steps:
      - name: Check out
        uses: actions/checkout@v7

      - name: Setup Copier Template
        uses: ./.github/actions/setup-copier-template
        with:
          template-language: ${{ matrix.template-type }}
          template-cloud-service: ${{ matrix.cloud-service }}
          resources-fixture: ${{ matrix.fixture }}

      - name: Setup Node ${{ matrix.node-version }} Environment
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}

      - run: corepack enable
        shell: bash

      - name: Run install
        run: yarn install --no-immutable
        working-directory: KittenClaws
        shell: bash

      - name: Run build
        run: yarn build
        working-directory: KittenClaws
        shell: bash

      - name: Run lint
        run: yarn lint
        working-directory: KittenClaws
        shell: bash

      - name: Unit tests
        run: yarn test:unit
        working-directory: KittenClaws
        shell: bash

      - name: Run dependency vulnerability scan
        continue-on-error: true
        run: |
            set +e
            yarn npm audit --severity moderate 2>&1 | tee audit.log
            status=$?
            set -e
            {
              echo "### TypeScript dependency vulnerability scan (${{ matrix.cloud-service }}, ${{ matrix.fixture }})"
              echo ""
              echo '```'
              cat audit.log
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
            exit $status
        working-directory: KittenClaws
        shell: bash
